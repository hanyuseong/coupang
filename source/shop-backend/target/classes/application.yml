spring:
  datasource:
    url: jdbc:mysql://localhost:3306/coupang?serverTimezone=Asia/Seoul
    username: root
    password: 12345

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true

jwt:
  secret: "your-secret-key-256"
  access-token-expiration: 3600000
  refresh-token-expiration: 1209600000

server:
  port: 8080